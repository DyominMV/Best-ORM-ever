package bestorm.filters;

import bestorm.Identifiable;

public interface ValueFilter<T> {
  /**
   * сравнение по полям
   *  = генерить выражения (id = ?)
   */

  // "SELECT * FROM dfkjhgkjfbg WHERE" + toString()


}